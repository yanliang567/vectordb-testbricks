# Milvus Upgrade/Rollback Gate 执行说明

该路径保留为兼容入口。当前唯一维护的执行指南在：

```text
../../../docs/upgrade-rollback-gates/README.md
```

请以该文档和 `../../manifests/upgrade_rollback_gates.yaml` 为准；其中包含当前
15 条 promoted gate、6 条 pre-release candidate 和 3 条 negative coverage
scenario。
