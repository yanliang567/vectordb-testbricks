# Legacy Collection API Bricks

Scripts in `milvus-bricks/` that still use `pymilvus.Collection`,
`connections`, and `utility` are legacy bricks. New MilvusClient-based
development should target `milvus-bricks/milvus_client`.
